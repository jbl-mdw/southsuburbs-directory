// frontend/components/Navigation.tsx
// Static Locations dropdown. Swap to Directus/geo later.

export default function Navigation() {
  const cities = [
    { name: "Matteson", slug: "matteson" },
    { name: "Homewood", slug: "homewood" },
    { name: "Flossmoor", slug: "flossmoor" },
    { name: "Olympia Fields", slug: "olympia-fields" },
    { name: "Park Forest", slug: "park-forest" },
    { name: "Chicago Heights", slug: "chicago-heights" },
  ];

  return (
    <nav className="flex items-center gap-6">
      {/* Explore Locations */}
      <div className="relative group">
        <button
          className="text-gray-700 hover:text-blue-600 font-medium"
          aria-haspopup="true"
          aria-expanded="false"
        >
          Explore Locations
        </button>
        <div className="absolute left-0 mt-2 w-64 bg-white rounded-lg shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
          <div className="py-2 max-h-96 overflow-y-auto">
            <a
              href="/cities"
              className="block px-4 py-2 text-gray-700 hover:bg-blue-50 hover:text-blue-600"
            >
              View All Cities →
            </a>
            {cities.map((c) => (
              <a
                key={c.slug}
                href={`/city/${c.slug}`}
                className="block px-4 py-2 text-gray-700 hover:bg-blue-50 hover:text-blue-600"
              >
                {c.name}
              </a>
            ))}
          </div>
        </div>
      </div>
    </nav>
  );
}
